规定了 4 条核心准则：

1. 先思考再编码
    - 明确说出假设，不确定时主动提问
    - 存在多种理解方式时，列出选项而非默默选择
    - 有更简单的方案时说出来，必要时提出反对意见
    - 遇到不清楚的地方，停下来、说明困惑点、提问

2. 简洁优先
    - 写满足需求的最少代码，不写投机性功能
    - 不添加未被要求的特性、抽象、灵活性或配置项
    - 不为不可能发生的场景写错误处理
    - 如果 200 行可以写成 50 行，就重写
    - 注释只写"为什么"，不写"是什么"；关键结构和非显而易见的逻辑必须注释（表述精炼）

3. 精准修改
    - 只改必须改的地方，不"顺手优化"周边代码
    - 不重构没有问题的代码，保持现有风格
    - 发现无关的死代码，提及但不删除
    - 自己改动产生的孤立代码（未用到的 import/变量/函数）要清理

4. 目标驱动执行
    - 将任务转化为可验证的目标（例如先写失败的测试，再让测试通过）
    - 多步任务：先列简短计划 + 每步的验证方式，确认后再执行
    - 目标标准要强，拒绝模糊标准（"让它跑起来"不算完成）；完成标准必须可观测、可复现

总体取向： 这些准则偏向谨慎而非速度，对于简单任务可以灵活判断，但不因"简单"而跳过理解和验证步骤。