# Android 控制 STM32 麦轮小车功能逻辑开发文档

## 一、项目目标

开发一套 Android BLE 遥控 STM32 麦轮小车系统。

系统实现：

- Android APP BLE控制
- STM32运动控制
- 麦轮运动控制
- PID调速
- BLE串口通信
- PWM电机控制

---

## 二、硬件架构

### 主控

- STM32F103C8T6

### 蓝牙模块

- JDY-23 BLE蓝牙模块

通信方式：

```text
BLE + UART
```

### 电机驱动

```text
双TB6612四路驱动板
```

### 电机

```text
520编码器减速电机 ×4
```

### 轮子

```text
麦克纳姆轮
```

---

## 三、系统通信结构

```text
Android APP
      │ BLE
JDY-23 BLE模块
      │ UART
STM32F103C8T6
      │ PWM + GPIO
TB6612驱动板
      │
四路电机
```

---

## 四、STM32 引脚定义

### 1. PWM输出

| 功能 | 引脚 | 定时器 |
|---|---|---|
| LEFT_PWM | PA0 | TIM2_CH1 |
| RIGHT_PWM | PA1 | TIM2_CH2 |

---

### 2. 左编码器

| 功能 | 引脚 |
|---|---|
| ENC_L_A | PA6 |
| ENC_L_B | PA7 |

使用：

```text
TIM3 Encoder Mode
```

---

### 3. 右编码器

| 功能 | 引脚 |
|---|---|
| ENC_R_A | PB6 |
| ENC_R_B | PB7 |

使用：

```text
TIM4 Encoder Mode
```

---

### 4. JDY-23 BLE接口

| JDY-23 | STM32 |
|---|---|
| TXD | PA3 |
| RXD | PA2 |
| VCC | 3.3V |
| GND | GND |

使用：

```text
USART2
9600波特率
```

---

### 5. 电机方向控制IO

#### 左前电机

| 功能 | 引脚 |
|---|---|
| LF_IN1 | PB12 |
| LF_IN2 | PB13 |

#### 左后电机

| 功能 | 引脚 |
|---|---|
| LR_IN1 | PB14 |
| LR_IN2 | PB15 |

#### 右前电机

| 功能 | 引脚 |
|---|---|
| RF_IN1 | PA4 |
| RF_IN2 | PA5 |

#### 右后电机

| 功能 | 引脚 |
|---|---|
| RR_IN1 | PB8 |
| RR_IN2 | PB9 |

---

## 五、电机控制逻辑

系统采用：

```text
左右共PWM
四轮独立方向控制
```

### PWM逻辑

#### 左侧共PWM

```text
左前PWM = 左后PWM
```

#### 右侧共PWM

```text
右前PWM = 右后PWM
```

---

## 六、运动控制逻辑

### 1. 前进

| 电机 | 方向 |
|---|---|
| 左前 | 正转 |
| 左后 | 正转 |
| 右前 | 正转 |
| 右后 | 正转 |

控制函数：

```c
Car_Forward(speed);
```

---

### 2. 后退

全部反转。

```c
Car_Backward(speed);
```

---

### 3. 左平移

| 电机 | 方向 |
|---|---|
| 左前 | 反转 |
| 左后 | 正转 |
| 右前 | 正转 |
| 右后 | 反转 |

```c
Car_LeftShift(speed);
```

---

### 4. 右平移

| 电机 | 方向 |
|---|---|
| 左前 | 正转 |
| 左后 | 反转 |
| 右前 | 反转 |
| 右后 | 正转 |

```c
Car_RightShift(speed);
```

---

### 5. 左旋转

```text
左侧后退
右侧前进
```

```c
Car_LeftRotate(speed);
```

---

### 6. 右旋转

```text
左侧前进
右侧后退
```

```c
Car_RightRotate(speed);
```

---

### 7. 停止

```c
Car_Stop();
```

---

### 8. 差速甩尾

```text
左右PWM不同
```

例如：

| 左PWM | 1000 |
|---|---|
| 右PWM | 300 |

```c
Car_Drift();
```

---

## 七、BLE通信协议

| 指令 | 功能 |
|---|---|
| F | 前进 |
| B | 后退 |
| L | 左平移 |
| R | 右平移 |
| G | 左旋转 |
| H | 右旋转 |
| S | 停止 |
| A | 加速 |
| D | 差速甩尾 |

---

## 八、STM32 软件逻辑

### 1. 初始化流程

```text
系统启动
   ↓
初始化GPIO
   ↓
初始化TIM2 PWM
   ↓
初始化TIM3编码器
   ↓
初始化TIM4编码器
   ↓
初始化USART2
   ↓
启动PWM
   ↓
启动编码器
   ↓
开启串口中断
```

---

### 2. BLE串口接收逻辑

```c
HAL_UART_Receive_IT()
```

---

### 3. 指令解析逻辑

```c
switch(rxData)
{
    case 'F':
        Car_Forward(speed);
        break;

    case 'B':
        Car_Backward(speed);
        break;

    case 'L':
        Car_LeftShift(speed);
        break;

    case 'R':
        Car_RightShift(speed);
        break;

    case 'G':
        Car_LeftRotate(speed);
        break;

    case 'H':
        Car_RightRotate(speed);
        break;

    case 'D':
        Car_Drift();
        break;

    case 'S':
        Car_Stop();
        break;
}
```

---

## 九、PWM控制逻辑

PWM范围：

```text
0 ~ 1000
```

推荐频率：

```text
15kHz
```

---

## 十、编码器测速逻辑

### 采样周期

```text
10ms
```

### 编码器读取

```c
left_speed = TIM3->CNT;
TIM3->CNT = 0;

right_speed = TIM4->CNT;
TIM4->CNT = 0;
```

---

## 十一、PID控制逻辑

PID公式：

u(t)=Kp*e(t)+Ki∫e(t)dt+Kd*de(t)/dt

---

## 十二、Android APP 功能逻辑

### APP功能

- BLE扫描
- BLE连接
- 控制按钮
- 速度控制
- 断线重连

---

## 十三、Android BLE流程

```text
打开APP
   ↓
扫描BLE设备
   ↓
找到JDY-23
   ↓
建立BLE连接
   ↓
发现服务
   ↓
发现特征值
   ↓
发送控制字符
```

---

## 十四、Android UI布局

```text
        前进

左平移   停止   右平移

        后退

左旋转   右旋转

加速     甩尾
```

---

## 十五、Android 控制逻辑

### 按下按钮

发送对应控制字符。

例如：

```text
前进按钮 → F
```

### 松开按钮

发送：

```text
S
```

---

## 十六、Android BLE发送逻辑

```kotlin
characteristic.value = "F".toByteArray()
gatt.writeCharacteristic(characteristic)
```

---

## 十七、Android BLE发送频率

```text
20ms ~ 50ms
```

---

## 十八、推荐代码结构

### STM32

```text
├── main.c
├── motor.c
├── motor.h
├── encoder.c
├── encoder.h
├── bluetooth.c
├── bluetooth.h
├── pid.c
├── pid.h
├── mecanum.c
├── mecanum.h
```

### Android

```text
├── MainActivity
├── BleManager
├── ControlView
├── BleService
├── CommandManager
```

---

## 十九、工程注意事项

### 1. JDY-23必须使用3.3V

禁止5V供电。

### 2. 必须缓启动

避免：

- 电池掉压
- STM32重启
- BLE断连

### 3. 左右共PWM会轻微跑偏

属于结构性问题。

---

## 二十、最终开发目标

### STM32部分

- CubeMX配置
- PWM驱动
- 电机控制
- 编码器测速
- PID控制
- BLE串口解析
- 麦轮运动控制

### Android部分

- BLE扫描
- BLE连接
- BLE发送
- UI控制界面
- 按钮事件
- 控制协议发送
